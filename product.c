#include <stdio.h>
int main() {
    double a, b, product;
    printf("Enter two numbers: ");
    scanf("%lf %lf", &a, &b);  
 
    // Calculating product
    product_nums = a * b;

    // %.2lf displays number up to 2 decimal point
    printf("Product = %.2lf", product_nums);
    
    return 0;
}
//this is my first file which i pushed on git hub
